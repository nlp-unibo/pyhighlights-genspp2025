"""What this reproduction implements rather than configures.

Almost nothing: the library holds the models, the metrics and the loaders, and
a reproduction is values. The exception is the released corpus's serialisation,
which predates the columns the library settled on.
"""
